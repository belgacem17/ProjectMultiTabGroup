import { PageMotherPipe } from './page-mother.pipe';

describe('PageMotherPipe', () => {
  it('create an instance', () => {
    const pipe = new PageMotherPipe();
    expect(pipe).toBeTruthy();
  });
});
